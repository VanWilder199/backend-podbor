# Тестовый профиль для @WebMvcTest — без БД
spring:
  flyway:
    enabled: false
  jooq:
    enabled: false

telegram:
  bot-token: test-token


s3:
  endpoint:  http://localhost:9000
  bucket: test-bucket
  access-key: testtest
  secret-key: testtest
  region: us-east-1
  presigned-url-ttl-minutes: 60